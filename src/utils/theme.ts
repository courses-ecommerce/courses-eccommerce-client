import { createTheme } from "@mui/material";

const theme = createTheme({
  spacing: 1,
  shape: { borderRadius: 4 },
  // typography: {
  //   fontFamily: "fantasy",
  // },
  //   palette: {
  //     mode: "light",
  //   },
});

export default theme;
